import { AppReducersModule } from './app-reducers.module';

describe('AppReducersModule', () => {
  it('should create an instance', () => {
    expect(new AppReducersModule()).toBeTruthy();
  });
});
